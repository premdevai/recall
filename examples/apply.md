# Application kit — Senior Frontend Engineer @ Northwind (example)

> Generated by `/recall apply <posting>`. Requirements matched against `.recall/evidence.json`;
> strong = direct accomplishment, partial = adjacent evidence, gap = stated honestly.

## Fit: 7 strong · 2 partial · 1 gap (of 10 requirements)

| Requirement | Match | Proof |
|---|---|---|
| 5+ yrs React at scale | **strong** | 3 yrs / 1,214 commits in `cobalt-web` + prior tenure; 212 merged PRs |
| Web performance ownership | **strong** | p75 LCP 5.2s → 2.1s (Datadog, PR #980); INP 480ms → 90ms (PR #1204) |
| Design-system experience | **strong** | Led token migration across 40 components (PROJ-701) |
| TypeScript | **strong** | 89% of authored diff since 2024 is TS (git) |
| Mentorship | **strong** | 1,200+ review threads; review playbook author |
| Testing culture | **strong** | Playwright migration; CI failure rate 18% → 3% |
| Accessibility | **strong** | WCAG audit remediation, 34 commits (PROJ-830) |
| GraphQL | *partial* | consumed via client hooks (12 commits); no schema design — say so if asked |
| Next.js / SSR | *partial* | adjacent: route-level code-splitting (PR #1090); no SSR in record |
| Native mobile exposure | **gap** | none — prepare: "deep web platform focus; mobile-web PWA work is the nearest evidence" |

## Résumé bullets — reordered for this posting

1. Cut p75 LCP 5.2s → 2.1s and INP 480ms → 90ms across a 1M-MAU React app (Datadog-verified).
2. Led design-token migration across 40 components with zero visual-regression escapes.
3. Drove review culture: 1,200+ threads, authored the team playbook, CI failures 18% → 3%.

## Cover letter fragments

- *Opening:* tie their "performance is a feature" line to the measured LCP/INP wins.
- *Middle:* the design-system migration as proof of cross-team execution.
- *Close:* review playbook = the mentorship they ask for, with receipts.

## Likely interview questions → your STAR story

1. "Walk me through a performance win" → **virtualization story** (PR #1204)
2. "A migration that went wrong?" → **token migration rollback moment** (PROJ-701)
3. "How do you raise a team's bar?" → **review playbook + CI numbers**
4. "Testing strategy?" → **Playwright migration story**
5. "Weakness in your stack?" → SSR gap — answer prepared above, don't bluff it

---
*Every number above traces to a commit, PR, ticket, or dashboard in the evidence file.
Nothing is invented; the gap stays a gap.*
